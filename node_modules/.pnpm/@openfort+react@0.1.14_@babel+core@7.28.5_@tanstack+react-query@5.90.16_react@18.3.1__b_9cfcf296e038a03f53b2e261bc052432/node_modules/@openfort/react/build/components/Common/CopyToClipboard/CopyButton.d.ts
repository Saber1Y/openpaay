import type React from 'react';
interface CopyButtonProps {
    value: string;
    children: React.ReactNode;
}
export declare const CopyButton: ({ value, children }: CopyButtonProps) => import("react/jsx-runtime").JSX.Element;
export {};
