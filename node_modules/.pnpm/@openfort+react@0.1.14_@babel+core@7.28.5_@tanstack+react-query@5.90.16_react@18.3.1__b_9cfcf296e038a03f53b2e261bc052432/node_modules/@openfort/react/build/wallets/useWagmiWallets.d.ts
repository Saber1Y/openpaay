import type { Connector } from 'wagmi';
import { type WalletConfigProps } from './walletConfigs';
export type WalletProps = {
    id: string;
    connector: Connector;
    isInstalled?: boolean;
} & WalletConfigProps;
export declare const useWallet: (id: string) => WalletProps | null;
export declare const useWagmiWallets: () => WalletProps[];
